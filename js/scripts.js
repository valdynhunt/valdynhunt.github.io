// Everything OK?
console.log('OK');

// Light Gallery
$('#gallery-1').lightGallery({
    thumbnail: false,
    share: false,
    download: false,
    mode: 'lg-slide',
    speed: 800,
    getCaptionFromTitleOrAlt: true
});
